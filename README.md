# Тестовое задание

## 🚀 Технологии
- Lombok
- БД (Postgres + docker compose)
- чистый JDBC + Hikari Connection Pool

## ⚙️ Конфигурация
1. Клонируйте репозиторий
2. Внесите свои данные в .env файл (Там логин, пароль и имя базы, поставил значения по умолчанию, можно их не менять)
3. Поднимите окружение проекта через docker команду:
```
docker-compose up -d
```
4. Запустите приложение через метод main (В консоли будет объяснение внутрянки)

## Функционал
- Сканирование нужных нам таблиц и их структуры (при помощи Groovy)
- Чтение содержимого каждой таблицы (при помощи Groovy)
- Динамическое создание DDL и SQL запроса на создание каждой таблицы
- Репозиторий для работы с базой
- Сервис для начала работы



